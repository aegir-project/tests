version: '2'

services:

  hostmaster:
    image: aegir/hostmaster
    ports:
      - 80:80
    hostname: aegir.docker
    links:
      - database
    depends_on:
      - database
    entrypoint:
      - docker-entrypoint-tests.sh
    environment:
      VIRTUAL_HOST: aegir.docker
      MYSQL_ROOT_PASSWORD: strongpassword
      AEGIR_MAKEFILE: /var/aegir/hostmaster-source/build-hostmaster.make
    tty: true
    stdin_open: true
    volumes:
      - ../:/var/aegir/tests
      - /home/travis/build/aegir-project/hostmaster:/var/aegir/hostmaster-source

  database:
    image: mariadb

    # Disable logging for this container, for testing environment.
    logging:
      driver: "none"
    environment:
      MYSQL_ROOT_PASSWORD: strongpassword
